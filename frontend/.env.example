# Base URL of the backend API (no trailing slash, no /api/v1 suffix - that's
# appended in chatApi.js). Vite only exposes vars prefixed with VITE_ to
# client-side code, and only reads .env at build/dev-server start time.
VITE_API_BASE_URL=http://localhost:8000
